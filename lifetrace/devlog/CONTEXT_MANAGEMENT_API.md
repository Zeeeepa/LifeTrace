# 上下文管理 API 文档

本文档描述了 LifeTrace 上下文管理功能的后端 API 实现。

## 概述

上下文管理功能允许将事件（Events）关联到任务（Tasks），从而建立工作活动与项目任务之间的联系。这使得用户可以：
- 查看哪些活动记录还未归类到具体任务
- 将活动记录关联到相应的任务
- 统计和分析任务相关的工作时间和内容

## 数据库设计

### events 表扩展

在现有的 `events` 表中添加了 `task_id` 字段：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| task_id | INTEGER | 关联的任务ID（外键 → tasks.id，可为空） |

完整的 events 表结构：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INTEGER | 事件ID（主键，自增） |
| app_name | VARCHAR(200) | 应用名称 |
| window_title | VARCHAR(500) | 窗口标题 |
| start_time | DATETIME | 开始时间 |
| end_time | DATETIME | 结束时间 |
| created_at | DATETIME | 创建时间 |
| ai_title | VARCHAR(50) | AI生成的事件标题 |
| ai_summary | TEXT | AI生成的事件摘要 |
| **task_id** | **INTEGER** | **关联的任务ID（新增）** |

## API 端点

### 1. 获取上下文列表

**端点**: `GET /api/contexts`

**查询参数**:
- `associated` (可选): 是否已关联任务
  - `false`: 只返回未关联的上下文
  - `true`: 只返回已关联的上下文
  - 不传: 返回所有上下文
- `task_id` (可选): 按任务ID过滤
- `limit` (可选): 返回数量限制（默认100，最大1000）
- `offset` (可选): 偏移量（默认0）

**响应**: HTTP 200 OK
```json
{
  "total": 309,
  "contexts": [
    {
      "id": 307,
      "app_name": "TestApp1",
      "window_title": "Test Window 1",
      "start_time": "2025-11-08T17:26:34.048496",
      "end_time": "2025-11-08T17:16:34.048496",
      "ai_title": null,
      "ai_summary": null,
      "task_id": null,
      "created_at": "2025-11-08T17:26:34.052531"
    }
  ]
}
```

**示例**:
```bash
# 获取所有未关联的上下文
curl -X GET "http://127.0.0.1:8000/api/contexts?associated=false"

# 获取所有已关联的上下文
curl -X GET "http://127.0.0.1:8000/api/contexts?associated=true"

# 获取关联到任务5的所有上下文
curl -X GET "http://127.0.0.1:8000/api/contexts?task_id=5"

# 获取所有上下文（不过滤）
curl -X GET "http://127.0.0.1:8000/api/contexts"

# 分页获取
curl -X GET "http://127.0.0.1:8000/api/contexts?limit=10&offset=20"
```

### 2. 获取单个上下文详情

**端点**: `GET /api/contexts/{context_id}`

**路径参数**:
- `context_id`: 上下文（事件）ID

**响应**: HTTP 200 OK
```json
{
  "id": 307,
  "app_name": "TestApp1",
  "window_title": "Test Window 1",
  "start_time": "2025-11-08T17:26:34.048496",
  "end_time": "2025-11-08T17:16:34.048496",
  "ai_title": null,
  "ai_summary": null,
  "task_id": null,
  "created_at": "2025-11-08T17:26:34.052531"
}
```

**错误响应**: HTTP 404 Not Found
```json
{
  "detail": "上下文记录不存在"
}
```

**示例**:
```bash
curl -X GET "http://127.0.0.1:8000/api/contexts/307"
```

### 3. 更新上下文的任务关联

**端点**: `PUT /api/contexts/{context_id}`

**路径参数**:
- `context_id`: 上下文（事件）ID

**请求体**:
```json
{
  "task_id": 5
}
```

**注意**:
- `task_id` 可以为 `null`，表示解除关联
- 如果指定了 `task_id`，会验证任务是否存在

**响应**: HTTP 200 OK
```json
{
  "id": 307,
  "app_name": "TestApp1",
  "window_title": "Test Window 1",
  "start_time": "2025-11-08T17:26:34.048496",
  "end_time": "2025-11-08T17:16:34.048496",
  "ai_title": null,
  "ai_summary": null,
  "task_id": 5,
  "created_at": "2025-11-08T17:26:34.052531"
}
```

**错误响应**:
- HTTP 404 Not Found (上下文不存在)
```json
{
  "detail": "上下文记录不存在"
}
```

- HTTP 404 Not Found (任务不存在)
```json
{
  "detail": "任务不存在"
}
```

**示例**:
```bash
# 将上下文关联到任务
curl -X PUT "http://127.0.0.1:8000/api/contexts/307" \
  -H "Content-Type: application/json" \
  -d '{"task_id": 5}'

# 解除上下文与任务的关联
curl -X PUT "http://127.0.0.1:8000/api/contexts/307" \
  -H "Content-Type: application/json" \
  -d '{"task_id": null}'
```

## 实现文件

### 1. 数据模型
- **文件**: `lifetrace/storage/models.py`
- **模型类**: `Event` (添加了 `task_id` 字段)
- **外键关系**: `task_id` → `tasks.id`

### 2. 数据库操作
- **文件**: `lifetrace/storage/database.py`
- **方法**:
  - `list_contexts(associated, task_id, limit, offset)`: 列出上下文记录
  - `count_contexts(associated, task_id)`: 统计上下文数量
  - `get_context(context_id)`: 获取单个上下文
  - `update_context_task(context_id, task_id)`: 更新上下文的任务关联

### 3. API Schema
- **文件**: `lifetrace/schemas/context.py`
- **模型类**:
  - `ContextResponse`: 上下文响应模型
  - `ContextListResponse`: 上下文列表响应模型
  - `ContextUpdateRequest`: 上下文更新请求模型

### 4. API 路由
- **文件**: `lifetrace/routers/context.py`
- **路由前缀**: `/api/contexts`

### 5. 服务器配置
- **文件**: `lifetrace/server.py`
- **路由注册**: `app.include_router(context.router)`

### 6. 数据库迁移
数据库初始化时会自动检测并添加 `task_id` 字段（如果不存在）：
```python
# 轻量级迁移：为已存在的 events 表添加 task_id 列
if self.database_url.startswith("sqlite:///"):
    with self.engine.connect() as conn:
        cols = [...]
        if "task_id" not in cols:
            conn.execute(text("ALTER TABLE events ADD COLUMN task_id INTEGER"))
```

## 核心特性

### 1. 灵活的过滤查询
- 获取所有未关联的上下文（`associated=false`）
- 获取所有已关联的上下文（`associated=true`）
- 按任务ID过滤上下文
- 支持分页查询

### 2. 任务关联管理
- 将上下文关联到任务
- 解除上下文与任务的关联
- 自动验证任务是否存在

### 3. 数据完整性
- 外键约束确保 `task_id` 的有效性
- 关联操作前验证任务存在
- 完善的错误处理

### 4. 向后兼容
- `task_id` 字段可为空
- 不影响现有事件记录
- 自动数据库迁移

## 测试结果

所有API端点已通过完整测试：

✅ **GET /api/contexts?associated=false** - 获取未关联的上下文  
✅ **GET /api/contexts?associated=true** - 获取已关联的上下文  
✅ **GET /api/contexts?task_id={id}** - 按任务过滤上下文  
✅ **GET /api/contexts** - 获取所有上下文  
✅ **GET /api/contexts/{context_id}** - 获取单个上下文  
✅ **PUT /api/contexts/{context_id}** - 关联上下文到任务  
✅ **PUT /api/contexts/{context_id}** (task_id=null) - 解除关联  
✅ **错误处理** - 404错误（上下文不存在、任务不存在）  

### 测试场景验证

1. ✅ 创建项目和任务
2. ✅ 创建测试事件（上下文）
3. ✅ 获取所有未关联的上下文
4. ✅ 获取单个上下文详情
5. ✅ 将上下文关联到任务
6. ✅ 验证关联成功
7. ✅ 获取已关联的上下文列表
8. ✅ 按任务ID过滤上下文
9. ✅ 解除上下文与任务的关联
10. ✅ 验证解除成功
11. ✅ 错误处理（任务不存在、上下文不存在）
12. ✅ 统计未关联上下文数量

## 使用场景

### 场景1：查看未归类的工作记录
```bash
# 获取所有未关联任务的上下文
curl -X GET "http://127.0.0.1:8000/api/contexts?associated=false"
```

### 场景2：将工作记录归类到任务
```bash
# 创建项目和任务
PROJECT_ID=1
TASK_ID=5

# 查找未归类的上下文
CONTEXTS=$(curl -s -X GET "http://127.0.0.1:8000/api/contexts?associated=false&limit=10")

# 将上下文307关联到任务5
curl -X PUT "http://127.0.0.1:8000/api/contexts/307" \
  -H "Content-Type: application/json" \
  -d '{"task_id": 5}'
```

### 场景3：查看任务相关的所有活动
```bash
# 获取任务5相关的所有上下文
curl -X GET "http://127.0.0.1:8000/api/contexts?task_id=5"
```

### 场景4：解除错误的关联
```bash
# 解除上下文307的任务关联
curl -X PUT "http://127.0.0.1:8000/api/contexts/307" \
  -H "Content-Type: application/json" \
  -d '{"task_id": null}'
```

## 完整工作流示例

```bash
# 1. 创建项目
PROJECT_RESP=$(curl -s -X POST "http://127.0.0.1:8000/api/projects" \
  -H "Content-Type: application/json" \
  -d '{"name": "Web开发项目", "goal": "开发企业网站"}')
PROJECT_ID=$(echo "$PROJECT_RESP" | python3 -c "import json, sys; print(json.load(sys.stdin)['id'])")

# 2. 创建任务
TASK_RESP=$(curl -s -X POST "http://127.0.0.1:8000/api/projects/$PROJECT_ID/tasks" \
  -H "Content-Type: application/json" \
  -d '{"name": "前端开发", "description": "开发用户界面"}')
TASK_ID=$(echo "$TASK_RESP" | python3 -c "import json, sys; print(json.load(sys.stdin)['id'])")

# 3. 查看未关联的上下文
curl -X GET "http://127.0.0.1:8000/api/contexts?associated=false&limit=10"

# 4. 将相关上下文关联到任务
# (假设找到了3个与前端开发相关的上下文: 101, 102, 103)
for context_id in 101 102 103; do
  curl -X PUT "http://127.0.0.1:8000/api/contexts/$context_id" \
    -H "Content-Type: application/json" \
    -d "{\"task_id\": $TASK_ID}"
done

# 5. 查看任务关联的所有上下文
curl -X GET "http://127.0.0.1:8000/api/contexts?task_id=$TASK_ID"

# 6. 统计未关联的上下文数量
curl -X GET "http://127.0.0.1:8000/api/contexts?associated=false&limit=1" | \
  python3 -c "import json, sys; print(f'未归类记录数: {json.load(sys.stdin)[\"total\"]}')"
```

## 数据库状态

### 表结构验证
```
✅ events 表已成功添加 task_id 字段
✅ task_id 字段类型：INTEGER（可为空）
✅ 外键约束：events.task_id → tasks.id
✅ 现有数据保持完整（task_id 为 NULL）
```

## 启动服务器

```bash
cd /Users/liji/Documents/LifeTrace
PYTHONPATH=/Users/liji/Documents/LifeTrace uv run python lifetrace/server.py
```

服务器将在 `http://127.0.0.1:8000` 启动。

## API 文档

启动服务器后，可以访问以下地址查看交互式API文档：
- Swagger UI: http://127.0.0.1:8000/docs
- ReDoc: http://127.0.0.1:8000/redoc

## 注意事项

1. **向后兼容**: `task_id` 字段可为空，不影响现有功能
2. **自动迁移**: 首次启动时自动添加 `task_id` 字段
3. **外键约束**: 关联的任务必须存在
4. **解除关联**: 传入 `task_id: null` 可解除关联
5. **数据完整性**: 删除任务时不会自动删除关联的上下文（需要手动处理或添加级联删除）
6. **性能考虑**: 大量数据时建议使用分页查询
7. **事件概念**: "上下文"在系统中对应"事件"（Event），是按应用使用区间聚合的截图集合

## 下一步建议

### 功能增强
- [ ] 添加批量关联功能
- [ ] 支持上下文的重新分类
- [ ] 添加上下文的时间范围过滤
- [ ] 实现自动分类建议（基于AI）

### 性能优化
- [ ] 添加数据库索引（task_id）
- [ ] 实现缓存机制
- [ ] 优化大量数据查询

### 前端开发
- [ ] 开发上下文列表界面
- [ ] 实现拖拽关联功能
- [ ] 显示任务关联的统计信息
- [ ] 实现时间线视图

## 总结

上下文管理功能成功实现了以下目标：

1. **数据模型扩展** - 在 events 表中添加 task_id 字段
2. **灵活查询** - 支持按关联状态和任务ID过滤
3. **关联管理** - 支持关联和解除关联操作
4. **数据验证** - 完善的输入验证和错误处理
5. **向后兼容** - 不影响现有功能
6. **自动迁移** - 无需手动数据库操作

所有功能都已经过完整测试，可以投入使用！🎉
